@model IEnumerable<BlogApp.Models.Post>

@{
    ViewData["Title"] = "Destinations";
}


<style>
/*     body {
        background-image: url("/img/IMG_2632.jpeg");
        background-repeat: no-repeat;
        background-attachment: fixed;
    } */

    body {
        background-image: url("/img/IMG_2632.jpeg");
        animation: moveIt 10s linear infinite alternate;
    }

    @@keyframes moveIt {
        from {
            background-position: 0 100%;
        }

        to {
            background-position: 100% 0;
        }
    }


    .card:hover {
        transform: scale(1.05);
        box-shadow: 0 10px 20px rgba(0,0,0,.12), 0 4px 8px rgba(0,0,0,.06);
    }

    .card {
        border-radius: 10px;
        background: rgba(0, 0, 0, 0.125);
        box-shadow: 0 6px 10px #000, 0 0 6px rgba(0, 0, 0, .05) ;
        transition: .3s transform cubic-bezier(.155,1.105,.295,1.12),.3s box-shadow,.3s -webkit-transform cubic-bezier(.155,1.105,.295,1.12);
        cursor: pointer;
    }

    .card-img-top {
        width: 100%;
        height: 15vw;
        object-fit: cover;
    }

</style>

@* 

<p>
    <a asp-action="Create">Create New</a>
</p> 
 <table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Body)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Created)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Body)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Created)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
           <a asp-action="Details" asp-route-id="@item.Id">Details</a> 
          <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
        </td>
        </tr>
}
    </tbody>
</table>
  *@


@* <div id="carouselExampleSlidesOnly" class="carousel slide" data-ride="carousel">
    <div class="carousel-inner">
        <div class="carousel-item active">
            <img class="d-block w-100" src="~/img/IMG_2277.jpeg" alt="First slide">
        </div>
        <div class="carousel-item">
            <img class="d-block w-100" src="~/img/IMG_2632.jpeg" alt="Second slide">
        </div>
        <div class="carousel-item">
            <img class="d-block w-100" src="~/img/about-bg.jpg" alt="Third slide">
        </div>
    </div>
</div> *@

<div class="row gx-4 gx-lg-5 ">
@foreach (var item in Model)
{

    <div class="col-md-4 mb-5">
            <div class="card bg-gradient" style="width: 18rem;">
                    <img class="card-img-top" src="~/@Html.DisplayFor(modelItem => item.ImagePath)" alt="Card image cap">
        <div class="card-body">
            <p class="card-text text-white">@Html.DisplayFor(modelItem => item.Title)</p>
        </div>
                <div class="card-footer"><a class="btn btn-outline-dark btn-sm" asp-action="Details" asp-route-id="@item.Id">Details</a></div>

    </div>
    </div>
}
</div>